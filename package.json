{
  "name": "plausiden-crawler",
  "version": "0.1.0",
  "description": "Headless-browser audit: click through any web UI, capture console/network/CSP issues",
  "private": true,
  "type": "module",
  "scripts": {
    "audit": "node --loader ts-node/esm src/main.ts",
    "audit:ui": "npx playwright test --ui",
    "install:deps": "npm install && npx playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@types/node": "^20.0.0",
    "playwright": "^1.45.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
