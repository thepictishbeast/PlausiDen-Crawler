{
  "name": "plausiden-site-feedback-form",
  "description": "End-to-end feedback form smoke. Lands on /feedback, fills the identity + general feedback section + the testimonial section with a synthetic-but-clearly-marked payload, submits, and asserts the acknowledgement page renders the expected confirmation copy. Catches three failure modes the unit tests can't: client-side script error blocking submit, server-side persistence/SMTP failure surfacing as non-2xx, and acknowledgement-page rendering regressions.",
  "baseUrl": "https://next.plausiden.com/",
  "viewport": { "w": 1440, "h": 900 },
  "steps": [
    { "kind": "goto", "url": "https://next.plausiden.com/feedback", "timeout": 15000, "label": "load-feedback" },
    { "kind": "wait", "ms": 600 },
    { "kind": "screenshot", "label": "01-feedback-form" },

    { "kind": "fill", "selector": "input[name='name']", "value": "PlausiDen Crawler", "label": "fill-name" },
    { "kind": "fill", "selector": "input[name='company']", "value": "Crawler Synthetic", "label": "fill-company" },
    { "kind": "fill", "selector": "input[name='email']", "value": "crawler-feedback@plausiden.com", "label": "fill-email" },

    { "kind": "fill", "selector": "textarea[name='worked_well']", "value": "Synthetic crawler submission — feedback form is reachable and the rendering pipeline is up.", "label": "fill-worked" },
    { "kind": "fill", "selector": "textarea[name='didnt_work']", "value": "Nothing — this is a synthetic test. Safe to ignore + delete from the export.", "label": "fill-didnt" },

    { "kind": "fill", "selector": "textarea[name='anything_else']", "value": "Synthetic submission from PlausiDen-Crawler smoke journey. Mark `consent=private` so it's never published.", "label": "fill-extra" },
    { "kind": "screenshot", "label": "02-feedback-filled" },

    { "kind": "click", "selector": "button[type='submit']", "timeout": 8000, "label": "submit" },
    { "kind": "wait", "ms": 2000, "label": "wait-for-ack" },
    { "kind": "screenshot", "label": "03-feedback-ack" },

    { "kind": "assertText", "selector": "body", "text": "Thank you" }
  ]
}
